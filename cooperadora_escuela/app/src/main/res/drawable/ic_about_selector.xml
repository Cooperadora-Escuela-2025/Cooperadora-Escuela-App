<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Ícono cuando está seleccionado -->
    <item android:drawable="@drawable/ic_about_selected" android:state_checked="true"/>
    <!-- Ícono por defecto (no seleccionado) -->
    <item android:drawable="@drawable/ic_about_unselected"/>
</selector>